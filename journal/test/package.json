{
  "name": "rojni-test",
  "private": true,
  "type": "module",
  "description": "Tests for Rojni. Kept out of the app folder on purpose: the app itself has no dependencies and no build step, and it should stay that way.",
  "scripts": {
    "test": "node smoke.mjs && node dom.mjs",
    "smoke": "node smoke.mjs",
    "dom": "node dom.mjs"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  }
}
